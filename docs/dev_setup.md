# XYLO — Developer Setup Guide  
### Local Installation, Running the Backend, Tools, and Developer Workflow

This guide explains **how to fully set up, run, and test XYLO** on a local machine.  
It covers environment setup, database initialization, running the API server, using automation features, and generating reports.

---

# 🧩 1. Requirements

Minimum recommended versions:

- **Python 3.10+**
- **pip** or **uv** (Python package manager)
- **Virtual environment** (venv/conda)
- **Git**
- Optional:
  - **Tesseract OCR** (future invoice OCR)
  - **Node.js** (if building frontend dashboard later)

---

# 📁 2. Clone the Repository

```bash
git clone https://github.com/Shashwat-Aneja/xylo.git
cd xylo
